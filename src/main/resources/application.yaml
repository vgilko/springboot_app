spring:
  datasource:
    url: jdbc:postgresql://localhost/denfinder
    username: denfinder_admin
    password: zxQWe.!/

  jpa:
    generate-ddl: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true